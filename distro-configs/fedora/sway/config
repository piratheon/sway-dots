# Fedora-specific Sway configuration overrides
# This would contain Fedora-specific settings if needed

# For Fedora systems, we might need different paths or packages
set $wallpaper_cmd "swww img"
set $terminal foot
set $browser firefox

# Fedora-specific keybindings could go here
bindsym $mod+Shift+r exec $term -e gnome-system-monitor

# Override any specific settings that differ on Fedora
output * bg /usr/share/wallpapers/fedora-logo.png fill