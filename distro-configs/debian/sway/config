# Debian-specific Sway configuration overrides
# This would contain Debian-specific settings if needed

# For Debian systems, we might need different paths or packages
set $wallpaper_cmd "swww img"
set $terminal foot
set $browser firefox

# Debian-specific keybindings could go here
bindsym $mod+Shift+r exec $term -e htop

# Override any specific settings that differ on Debian
output * bg /usr/share/backgrounds/debian-default.png fill